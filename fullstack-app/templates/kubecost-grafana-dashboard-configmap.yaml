apiVersion: v1
kind: ConfigMap
metadata:
  name: kubecost-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  kubecost-dashboard.json: |
    {{ .Files.Get "dashboards/kubecost-dashboard.json" | indent 4 }}

